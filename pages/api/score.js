/* import clientPromise from "../lib/mongodb";

async function updateScores(data) {
  const updatedScores = [];

  // Calculate scores for each fixture
  data.forEach((fixture) => {
    // Calculate scores for match one
    const matchOneHomeScore = fixture.matchOne.home.score;
    const matchOneAwayScore = fixture.matchOne.away.score;
    if (matchOneHomeScore > matchOneAwayScore) {
      // Home team wins
      updatedScores.push({
        team: fixture.matchOne.home.team,
        score: 3,
      });
      updatedScores.push({
        team: fixture.matchOne.
 */
